# Environment variables template for Service Principal authentication
# Copy this file to .env and fill in your actual values
# DO NOT COMMIT the .env file - it contains sensitive credentials

# Service Principal Authentication (required for Terraform and Azure CLI)
ARM_CLIENT_ID=your-service-principal-client-id
ARM_CLIENT_SECRET=your-service-principal-client-secret  
ARM_TENANT_ID=your-azure-tenant-id
ARM_SUBSCRIPTION_ID=your-azure-subscription-id

# Terraform specific settings
ARM_USE_MSI=false
ARM_ENVIRONMENT=public

# Optional: Set default location
ARM_LOCATION=eastus2

# Instructions:
# 1. Copy this file: cp .env.template .env
# 2. Replace the placeholder values with your actual Service Principal credentials
# 3. Source the file: source .env
# 4. Run: az login --service-principal -u $ARM_CLIENT_ID -p $ARM_CLIENT_SECRET --tenant $ARM_TENANT_ID
# 5. Initialize Terraform: terraform init
# 6. Deploy: terraform plan && terraform apply